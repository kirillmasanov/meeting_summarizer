# 🎥 Meeting Summarizer

Веб-приложение для автоматической обработки видеозаписей встреч с извлечением аудио, транскрибацией через Yandex SpeechKit API и суммаризацией на основе пользовательского системного промпта.

## 🌟 Возможности

- **Загрузка видео**: Drag-and-drop интерфейс для загрузки видеофайлов (MP4, WebM, до 1GB)
- **Извлечение аудио**: Автоматическое извлечение аудиодорожки из видео с помощью ffmpeg
- **Транскрибация**: Преобразование речи в текст через Yandex SpeechKit API v3
- **Суммаризация**: Создание краткого резюме встречи с помощью Qwen 3 (qwen3-235b-a22b-fp8)
- **Настраиваемый промпт**: Возможность задать собственные инструкции для суммаризации
- **Индикатор загрузки**: Визуальный спиннер с отображением текущей стадии обработки
- **Копирование результатов**: Быстрое копирование резюме в буфер обмена
- **Автоматическое удаление**: Файлы и статусы удаляются сразу после получения результата

## 📋 Требования

### Системные зависимости

- **Python 3.8+**
- **ffmpeg** - для извлечения аудио из видео
  - macOS: `brew install ffmpeg`
  - Ubuntu/Debian: `sudo apt-get install ffmpeg`
  - Windows: скачайте с [ffmpeg.org](https://ffmpeg.org/download.html)

### Yandex Cloud

- Аккаунт в [Yandex Cloud](https://cloud.yandex.ru/)
- API ключ для доступа к Yandex SpeechKit
- ID каталога (Folder ID)

## 🚀 Установка

**Установка uv:**
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

**Установка проекта:**
```bash
git clone <repository-url>
cd meeting_summarizer

# Создание виртуального окружения и установка зависимостей
uv sync
```

**Примечание:** При использовании `uv` не требуется вручную активировать виртуальное окружение. Команда `uv run` автоматически использует созданное окружение.

### Настройка Yandex Cloud

#### 1. Получение Folder ID

Folder ID можно получить через CLI:
```bash
yc config get folder-id
```

Или в веб-консоли:
1. Перейдите в [консоль Yandex Cloud](https://console.cloud.yandex.ru/)
2. Выберите ваш каталог
3. ID каталога отображается в URL или в информации о каталоге

#### 2. Создание сервисного аккаунта

**Через веб-консоль:**
1. Перейдите в раздел "Сервисные аккаунты"
2. Нажмите "Создать сервисный аккаунт"
3. Укажите имя (например, `meeting-summarizer-sa`)
4. Добавьте описание (опционально)

**Через CLI:**
```bash
# Создание сервисного аккаунта
yc iam service-account create \
  --name meeting-summarizer-sa \
  --description "Service account for meeting summarizer"

# Получение ID созданного аккаунта
SA_ID=$(yc iam service-account get meeting-summarizer-sa --format json | jq -r '.id')
echo "Service Account ID: $SA_ID"
```

#### 3. Назначение ролей

Сервисному аккаунту необходимы следующие роли:
- `ai.speechkit-stt.user` - для работы с SpeechKit API (транскрибация)
- `ai.languageModels.user` - для работы с LLM (суммаризация)

**Через веб-консоль:**
1. Откройте страницу каталога
2. Перейдите на вкладку "Права доступа"
3. Нажмите "Назначить роли"
4. Выберите сервисный аккаунт
5. Добавьте обе роли: `ai.speechkit-stt.user` и `ai.languageModels.user`

**Через CLI:**
```bash
# Получите FOLDER_ID
FOLDER_ID=$(yc config get folder-id)

# Назначьте роли
yc resource-manager folder add-access-binding $FOLDER_ID \
  --role ai.speechkit-stt.user \
  --service-account-id $SA_ID

yc resource-manager folder add-access-binding $FOLDER_ID \
  --role ai.languageModels.user \
  --service-account-id $SA_ID
```

#### 4. Создание API ключа

**Через веб-консоль:**
1. Откройте страницу сервисного аккаунта
2. Перейдите на вкладку "API-ключи"
3. Нажмите "Создать API-ключ"
4. Скопируйте ключ (он показывается только один раз!)

**Через CLI:**
```bash
# Создание API-ключа
yc iam api-key create --service-account-id $SA_ID --format json

# Сохранение ключа в переменную
API_KEY=$(yc iam api-key create --service-account-id $SA_ID --format json | jq -r '.secret')
echo "API Key: $API_KEY"
```

#### 5. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
YANDEX_API_KEY=your_api_key_here
FOLDER_ID=your_folder_id_here
LLM_MODEL=qwen3-235b-a22b-fp8/latest
SERVER_PORT=8000
```

**Примечание:** Параметры `LLM_MODEL` и `SERVER_PORT` опциональны. Если не указаны, будут использованы значения по умолчанию.

**Автоматическое создание .env через CLI:**
```bash
# Получение значений
FOLDER_ID=$(yc config get folder-id)
SA_ID=$(yc iam service-account get meeting-summarizer-sa --format json | jq -r '.id')
API_KEY=$(yc iam api-key create --service-account-id $SA_ID --format json | jq -r '.secret')

# Создание .env файла
cat > .env << EOF
YANDEX_API_KEY=$API_KEY
FOLDER_ID=$FOLDER_ID
LLM_MODEL=qwen3-235b-a22b-fp8/latest
SERVER_PORT=8000
EOF

echo "Файл .env создан успешно"
```

### Проверка установки ffmpeg

```bash
ffmpeg -version
```

Если команда выполнена успешно, ffmpeg установлен корректно.

## 🎯 Запуск приложения

```bash
uv run python app.py
```

Приложение будет доступно по адресу: **http://localhost:8000**

### Режим разработки с автоперезагрузкой:
```bash
uv run uvicorn app:app --reload
```

### Проверка работоспособности

Откройте в браузере: **http://localhost:8000**

Вы должны увидеть веб-интерфейс с формой загрузки файлов и текстовым полем для системного промпта.

## 📖 Использование

### Веб-интерфейс

1. Откройте **http://localhost:8000** в браузере
2. Перетащите видеофайл в область загрузки или нажмите для выбора
3. (Опционально) Отредактируйте системный промпт для суммаризации
4. Нажмите "Загрузить и обработать"
5. Дождитесь завершения обработки (отображается спиннер с текущей стадией)
6. Просмотрите резюме встречи
7. Скопируйте результаты в буфер обмена при необходимости

### API эндпоинты

#### Загрузка видео
```bash
POST /api/upload
Content-Type: multipart/form-data

file: <video_file>
system_prompt: <optional_custom_prompt>
```

#### Проверка статуса
```bash
GET /api/status/{task_id}
```

#### Получение результата
```bash
GET /api/result/{task_id}
```

#### Получение системного промпта
```bash
GET /api/prompt
```

## 🏗️ Структура проекта

```
meeting_summarizer4/
├── app.py                      # Основное FastAPI приложение
├── pyproject.toml              # Конфигурация проекта и зависимости
├── .env                        # Переменные окружения (не в git)
├── README.md                   # Документация
├── system_prompt.txt           # Системный промпт для суммаризации
├── static/                     # Статические файлы
│   └── index.html             # Веб-интерфейс
├── uploads/                    # Загруженные видео (создается автоматически)
└── temp/                       # Временные файлы (создается автоматически)
```

## ⚙️ Конфигурация

### Ограничения

- **Максимальный размер файла**: 1 GB
- **Максимальная длительность видео**: 1 час (60 минут)
- **Поддерживаемые форматы**: MP4, WebM
- **Хранение данных**: Файлы и статусы удаляются сразу после получения результата
- **Аудио формат**: Автоматическое преобразование в MP3

### Настройка переменных окружения

Приложение поддерживает следующие переменные окружения в файле `.env`:

- `YANDEX_API_KEY` (обязательно) - API ключ для Yandex Cloud
- `FOLDER_ID` (обязательно) - ID каталога в Yandex Cloud
- `LLM_MODEL` (опционально, по умолчанию: `qwen3-235b-a22b-fp8/latest`) - модель для суммаризации
- `SERVER_PORT` (опционально, по умолчанию: `8000`) - порт для запуска сервера

### Настройка системного промпта

Системный промпт определяет, как будет создаваться резюме встречи. Промпт загружается из файла `system_prompt.txt` при запуске приложения.

Вы можете изменить промпт:
- Отредактировав файл `system_prompt.txt` и перезапустив приложение
- Через веб-интерфейс перед загрузкой каждого файла

## 🐛 Устранение неполадок

### Ошибка "ffmpeg не найден"

Убедитесь, что ffmpeg установлен и доступен в PATH:
```bash
which ffmpeg
```

### Ошибка "API key not configured"

Проверьте файл `.env`:
- Убедитесь, что файл существует в корне проекта
- Проверьте правильность API ключа
- Убедитесь, что нет лишних пробелов

### Ошибка при транскрибации или суммаризации

- Проверьте, что у сервисного аккаунта есть обе роли: `ai.speechkit-stt.user` и `ai.languageModels.user`
- Убедитесь, что Folder ID указан правильно
- Проверьте квоты в Yandex Cloud

### Большое время обработки

Время обработки зависит от:
- Длительности видео
- Размера файла
- Загрузки API Yandex Cloud

Обычно обработка занимает 1-3 минуты для видео длительностью 10-15 минут.

## 📝 Примеры использования

### Пример 1: Обработка записи Zoom встречи

```bash
# 1. Скачайте запись встречи из Zoom (формат MP4)
# 2. Откройте веб-интерфейс
# 3. Загрузите файл
# 4. Получите транскрипцию и резюме
```

### Пример 2: Обработка записи Яндекс Телемост

```bash
# 1. Скачайте запись из Телемоста (формат WebM)
# 2. Используйте веб-интерфейс или API
# 3. Настройте промпт под формат ваших встреч
```

### Пример 3: Автоматизация через API

```python
import requests

# Загрузка файла
with open('meeting.mp4', 'rb') as f:
    files = {'file': f}
    response = requests.post('http://localhost:8000/api/upload', files=files)
    task_id = response.json()['task_id']

# Проверка статуса
import time
while True:
    status = requests.get(f'http://localhost:8000/api/status/{task_id}').json()
    if status['status'] == 'completed':
        break
    time.sleep(5)

# Получение результата
result = requests.get(f'http://localhost:8000/api/result/{task_id}').json()
print(result['summary'])
```

## 🤝 Вклад в проект

Если вы хотите внести вклад в проект:

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

MIT License

## 👥 Авторы

Разработано для автоматизации обработки видеозаписей встреч с использованием Yandex Cloud AI.

## 🔗 Полезные ссылки

- [Yandex SpeechKit Documentation](https://cloud.yandex.ru/docs/speechkit/)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [FFmpeg Documentation](https://ffmpeg.org/documentation.html)

## 📞 Поддержка

При возникновении проблем:
1. Проверьте раздел "Устранение неполадок"
2. Убедитесь, что все зависимости установлены
3. Проверьте логи приложения
4. Создайте Issue в репозитории